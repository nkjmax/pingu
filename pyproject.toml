[project]
name = "pingu"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "nkjmax", email = "ngkangjuin@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "aiosqlite>=0.22.1",
    "apscheduler>=3.11.3",
    "discord>=2.3.2",
    "groq>=1.6.0",
    "python-dateutil>=2.9.0.post0",
    "python-dotenv>=1.2.2",
]

[project.scripts]
pingu = "pingu:main"

[build-system]
requires = ["uv_build>=0.12.1,<0.13.0"]
build-backend = "uv_build"
